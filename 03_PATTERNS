problem 41 - finding digit of number
solution-
#include<iostream>
using namespace std;
int main()
{
	int n;
	cout << " Entre the value of n : ";
	cin >> n;
	int count = 0;
	while (n != 0) {
		n = n / 10;
		count++;	
	}
	cout << count;
}

=================================================

problem 42 - printing rectangle by getting rows as input 
solution -
#include<iostream>
using namespace std;
int main()
{
	int m;
	cout << " Entre number of rows  : ";
	cin >> m;
	for (int i = 1;i <= m;i++)
	{
		cout << "*****" << endl;
	}
}

=================================================

